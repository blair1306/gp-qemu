<dec f='codebrowser/migration/savevm.h' l='45' type='int qemu_savevm_send_packaged(QEMUFile * f, const uint8_t * buf, size_t len)'/>
<use f='codebrowser/migration/migration.c' l='1776' u='c' c='postcopy_start'/>
<def f='codebrowser/migration/savevm.c' l='841' ll='859' type='int qemu_savevm_send_packaged(QEMUFile * f, const uint8_t * buf, size_t len)'/>
<doc f='codebrowser/migration/savevm.c' l='832'>/* We have a buffer of data to send; we don&apos;t want that all to be loaded
 * by the command itself, so the command contains just the length of the
 * extra buffer that we then send straight after it.
 * TODO: Must be a better way to organise that
 *
 * Returns:
 *    0 on success
 *    -ve on error
 */</doc>
