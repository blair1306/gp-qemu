<def f='codebrowser/include/qemu/timer.h' l='476' ll='480' type='void timer_init_us(QEMUTimer * ts, QEMUClockType type, QEMUTimerCB * cb, void * opaque)'/>
<doc f='codebrowser/include/qemu/timer.h' l='463'>/**
 * timer_init_us:
 * @ts: the timer to be initialised
 * @type: the clock to associate with the timer
 * @cb: the callback to call when the timer expires
 * @opaque: the opaque pointer to pass to the callback
 *
 * Initialize a timer with microsecond scale on the default timer list
 * associated with the clock.
 *
 * You need not call an explicit deinit call. Simply make
 * sure it is not on a list with timer_del.
 */</doc>
<use f='codebrowser/net/filter-buffer.c' l='109' u='c' c='filter_buffer_setup_timer'/>
