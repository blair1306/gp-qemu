<def f='codebrowser/include/qemu/hbitmap.h' l='336' ll='352' type='size_t hbitmap_iter_next_word(HBitmapIter * hbi, unsigned long * p_cur)'/>
<doc f='codebrowser/include/qemu/hbitmap.h' l='325'>/**
 * hbitmap_iter_next_word:
 * @hbi: HBitmapIter to operate on.
 * @p_cur: Location where to store the next non-zero word.
 *
 * Return the index of the next nonzero word that is set in @hbi&apos;s
 * associated HBitmap, and set *p_cur to the content of that word
 * (bits before the index that was passed to hbitmap_iter_init are
 * trimmed on the first call).  Return -1, and set *p_cur to zero,
 * if all remaining words are zero.
 */</doc>
<use f='codebrowser/util/hbitmap.c' l='219' u='c' c='hb_count_between'/>
