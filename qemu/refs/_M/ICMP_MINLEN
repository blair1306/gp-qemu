<dec f='codebrowser/slirp/ip_icmp.h' l='101'/>
<doc f='codebrowser/slirp/ip_icmp.h' l='93'>/*
 * Lower bounds on packet lengths for various types.
 * For the error advice packets must first ensure that the
 * packet is large enough to contain the returned ip header.
 * Only then can we do the check to see if 64 bits of packet
 * data have been returned, since we need to check the returned
 * ip header length.
 */</doc>
<use f='codebrowser/slirp/ip_icmp.c' l='136' u='c'/>
<use f='codebrowser/slirp/ip_icmp.c' l='288' u='c'/>
<use f='codebrowser/slirp/ip_icmp.c' l='304' u='c'/>
<use f='codebrowser/slirp/ip_icmp.c' l='308' u='c'/>
