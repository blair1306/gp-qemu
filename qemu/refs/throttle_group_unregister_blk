<dec f='codebrowser/include/block/throttle-groups.h' l='40' type='void throttle_group_unregister_blk(BlockBackend * blk)'/>
<use f='codebrowser/block/block-backend.c' l='1917' u='c' c='blk_io_limits_disable'/>
<def f='codebrowser/block/throttle-groups.c' l='531' ll='560' type='void throttle_group_unregister_blk(BlockBackend * blk)'/>
<doc f='codebrowser/block/throttle-groups.c' l='521'>/* Unregister a BlockBackend from its group, removing it from the list,
 * destroying the timers and setting the throttle_state pointer to NULL.
 *
 * The BlockBackend must not have pending throttled requests, so the caller has
 * to drain them first.
 *
 * The group will be destroyed if it&apos;s empty after this operation.
 *
 * @blk: the BlockBackend to remove
 */</doc>
